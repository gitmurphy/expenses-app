function App() {
  return (
    <div>
      <h2>Let's get started!</h2>
    </div>
  );
}

export default App;

// export the App function (component) so that it may be imported in other files, i.e. index.js